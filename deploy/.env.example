# Copy to deploy/.env to override the development defaults used by docker-compose.yml.
# Development-only values; never reuse them in a deployed environment (ADR-0016).
POSTGRES_PASSWORD=orderplatform-dev
KEYCLOAK_ADMIN_PASSWORD=admin
